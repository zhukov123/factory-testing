# RateLimiter Tests

This directory is reserved for the xUnit suite that validates the C# rate limiter implementations. When tests are added, run them with:

```bash
dotnet test tests/RateLimiter.Tests/RateLimiter.Tests.csproj
```

Feel free to add fixtures, coverage instrumentation, or benchmarking helpers alongside the test project.
