# URL Shortener

Document the URL shortener's design, assumptions, and upgrade path in this folder. The core implementation lives in `projects/url-shortener/src/url_shortener.py`, and unit tests live under `projects/url-shortener/tests`.
